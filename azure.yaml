# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: carddemo-azure
metadata:
  template: carddemo-azure@1.0.0
infra:
  provider: bicep
  path: ./infra
services:
  web:
    project: .
    host: containerapp
    language: docker
    apiVersion: 2026-01-01
    docker:
      path: ./Dockerfile
      context: .
